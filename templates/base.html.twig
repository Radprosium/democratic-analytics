<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Democratic Analytics{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🏛️</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        <header class="site-header">
            <nav class="nav-container">
                <a href="{{ path('app_home') }}" class="nav-brand">
                    <span class="nav-icon">🏛️</span>
                    <span>Democratic Analytics</span>
                </a>
                <div class="nav-links">
                    <a href="{{ path('app_home') }}" class="nav-link{% if app.request.attributes.get('_route') == 'app_home' %} active{% endif %}">Home</a>
                    <a href="{{ path('app_subject_index') }}" class="nav-link{% if app.request.attributes.get('_route') starts with 'app_subject' %} active{% endif %}">Subjects</a>
                </div>
            </nav>
        </header>

        <main class="main-content">
            {% block body %}{% endblock %}
        </main>

        <footer class="site-footer">
            <div class="footer-container">
                <p>Democratic Analytics — AI-powered political analysis for informed citizens</p>
            </div>
        </footer>
    </body>
</html>
